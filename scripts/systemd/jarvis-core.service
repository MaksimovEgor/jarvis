[Unit]
Description=Jarvis core (FastAPI: STT → agent → TTS)
After=network-online.target

[Service]
Type=simple
WorkingDirectory=%h/jarvis
ExecStart=%h/jarvis/.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
